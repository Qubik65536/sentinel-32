[package]
name = "sentinel-app"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[features]
default = []
openai-test = ["sentinel-ai-check/openai-test"]

[dependencies]
sentinel-ai-check = { path = "../sentinel-ai-check" }
sentinel-core = { path = "../sentinel-core" }
sentinel-scenario = { path = "../sentinel-scenario" }
serde_json = "1"

[lints]
workspace = true
